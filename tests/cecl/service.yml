kind: Service
apiVersion: v1
metadata:
  name: web-service-moodle01
  namespace: siad-moodlececl-dev-01
spec:
  selector:
    app: ame01-prod
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
      nodePort: 30047
      name: http
  type: NodePort  
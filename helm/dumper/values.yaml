
namespace: siad-moodlececl-dev-01
pvc: pvc-siad-moodlececl-dev-01-vol01
secrets: secrets-moodle01-dev
bckdate: 2017-08-28_12:14:53
# Seulement si <> de /mnt/NFS
# pour moodleame /var/www/moodledata
# dataroot: /var/www
# bd: cecl # valeur par défaut


##### Prod
# namespace: siad-moodlececl-prod-01
# pvc: pvc-siad-moodlececl-prod-01-vol01
# bd: moodleame



#################################################################
# Par paresse, on laisse les valeurs originalles.
# Quand on sera rendu au refactoring ce seront nettoyés.
# 
# ####################################################
# Default values for dumper.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
image:
  repository: nginx
  tag: stable
  pullPolicy: IfNotPresent
service:
  name: nginx
  type: ClusterIP
  externalPort: 80
  internalPort: 80
ingress:
  enabled: false
  # Used to create an Ingress record.
  hosts:
    - chart-example.local
  annotations:
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  tls:
    # Secrets must be manually created in the namespace.
    # - secretName: chart-example-tls
    #   hosts:
    #     - chart-example.local
resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #  cpu: 100m
  #  memory: 128Mi
  # requests:
  #  cpu: 100m
  #  memory: 128Mi
